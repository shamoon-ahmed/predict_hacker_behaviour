# predict_hacker_behaviour
Built a ML model using DecisionTreeClassifier that can detect whether a user/IP address is malicious or benigne based on how they interact with the fake server called honeypot.
